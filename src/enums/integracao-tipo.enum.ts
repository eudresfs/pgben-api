/**
 * Enum que define os tipos de integrações externas suportadas pelo sistema.
 * Cada tipo de integração pode ter configurações e parâmetros específicos.
 */
export enum IntegracaoTipoEnum {
  EMAIL = 'email',
  STORAGE = 'storage',
  SMS = 'sms',
  API_EXTERNA = 'api_externa'
}
