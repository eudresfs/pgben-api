/**
 * Enum que define os tipos de templates suportados pelo sistema.
 * Cada tipo de template pode ter variáveis e contextos específicos disponíveis.
 */
export enum TemplateTipoEnum {
  EMAIL = 'email',
  NOTIFICACAO = 'notificacao',
  DOCUMENTO = 'documento',
}
