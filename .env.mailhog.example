# Configuração para MailHog (Desenvolvimento)
# MailHog é um servidor SMTP de teste que captura emails sem enviá-los

# Configurações básicas do email
EMAIL_ENABLED=true
NODE_ENV=development

# Configurações SMTP para MailHog
SMTP_HOST=localhost
SMTP_PORT=1025

# MailHog não requer autenticação - deixe vazio ou remova
SMTP_USER=
SMTP_PASS=

# Configurações do remetente
SMTP_FROM=noreply@localhost.test
SMTP_FROM_NAME=SEMTAS - Sistema de Desenvolvimento

# Email de suporte
SUPPORT_EMAIL=suporte@semtas.gov.br

# URL do frontend para links nos emails
FRONTEND_URL=http://localhost:3000

# Configurações de segurança (não necessárias para MailHog)
SMTP_REJECT_UNAUTHORIZED=false

# Como iniciar o MailHog:
# Docker: docker run -d -p 1025:1025 -p 8025:8025 mailhog/mailhog
# Interface web: http://localhost:8025
# Porta SMTP: 1025